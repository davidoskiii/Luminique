let x = 0;
println("Creating x mutable variable: ${x}");

let y;
println("Creating y mutable variable (not initialized): ${y}");

const z = true;
println("Creating z immutable variable: ${z}");

// z = false;

// const j;

print("Initializing more than one variable at once: ");
let a, b, c = 1, 2, 3;

println("${a}, ${b}, ${c}");

// const d, e, f;

x = 0;
println("Setting x equal to 0: ${x}");
x += 3;
println("Incrementing x by 3 using += operator: ${x}");
