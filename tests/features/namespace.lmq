namespace lmq::tests;

using luminique::std;
using tests::features::fraction;
using tests::features::fib;

println(std::chrono::Date);
println(Fraction);
println(fibonacci);

let frac = Fraction(3, 6);
let frac2 = frac.reduce();

println(frac.toString());
println(frac2.toString());
println(frac == frac2);

// println(Date); Error!
