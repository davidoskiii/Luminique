let exc = Exception("This is the error message");

println("The error message of 'exc' is: ${exc.message}.");

try {
  throw exc;
} catch (Exception e){ 
  println("Caught exception: ${e.message}.");
}

throw exc;
