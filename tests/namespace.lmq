namespace lmq.tests;

using luminique.std.collection as cl;
using luminique.std;
using tests.fraction;
using tests.fib;

println(cl::Array);
println(std::util::Date);
println(Fraction);
println(fibonacci);

let frac = Fraction(3, 6);
let frac2 = frac.reduce();

println(frac.toString());
println(frac2.toString());
println(frac == frac2);

// println(Date); Error!
