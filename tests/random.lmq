using luminique.std.util.Random;

let rand = Random();

println("Generating random numbers between 0 to 10000: ");
println("Current seed is ${rand.getSeed()}");

let i = 0;

while (i < 100) { 
    i = i + 1;
    print("Random number ${i}: ");
    println(rand.nextIntBounded(10000));
}
